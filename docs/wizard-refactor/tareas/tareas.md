# Tareas: refactor de configuración inicial del hotel — índice

Fuente de verdad: `../00-resumen-ejecutivo.md` a `../08-decisiones-abiertas-y-riesgos.md`
(ya reflejan las correcciones del usuario del 2026-09-07). Este índice no
reinterpreta ninguna decisión — solo organiza `../07-plan-fases-implementacion.md`
en tareas chicas, **una por archivo**, cada una completable en una sola sesión,
con su criterio de aceptación explícito. Mismo formato que ya usa el proyecto en
`openspec/changes/*/tasks.md`.

**Orden obligatorio: F0 → F1 → F2 → F3 → F4 → F5.** Dentro de cada fase, varias
tareas son independientes entre sí — se marca la dependencia real dentro de
cada archivo de tarea, no hay que asumir que el orden numérico es siempre una
dependencia dura.

Cada fase termina con un **Gate de verificación obligatorio**:
```
cd backend && bun run typecheck
cd backend && bun run node_modules/arckode-framework/bin/arckode.js analyze   # ✅ VÁLIDO, 0 violaciones
cd backend && bun test
cd frontend && bun run typecheck   # vue-tsc -b
cd frontend && bun run build       # termina en "✓ built"
```

## F0 — Confirmación y reproducción (sin código de producción)

> Tareas 0.1 a 0.5 empaquetadas en un solo archivo para entregar a una IA de
> trabajo — completadas, ver [done/paquete-01-tareas-1-10.md](./done/paquete-01-tareas-1-10.md).

- [x] 0.1 — Reproducir el bug de guardado de tipo/estrellas (confirmado en vivo, ver auditoría 01 sección 3.3)
- [x] 0.2 — Confirmar endpoint de editar perfil de usuario (`PUT /api/auth/me`, ya existe)
- [x] 0.3 — Confirmar fuente de centros lat/lng por país (no existe, se crea desde cero)
- [x] 0.4 — Investigar duplicado de amenities (D3 resuelta: duplicación real, se unifica en `hotel_amenities`)
- [x] 0.5 — Cerrar D5 y D6 con el usuario (D5: la franja del dashboard NO se puede ocultar/descartar; D6: nombre final "Centro de configuración" — ver doc 08)

## F1 — Mover Ubicación (y campos públicos sueltos) a Página pública

> Tareas 1.1 a 1.5 (primer tramo de F1) también estaban en
> [done/paquete-01-tareas-1-10.md](./done/paquete-01-tareas-1-10.md), junto con F0 —
> completadas. El resto de F1 (1.6 en adelante) sigue en archivos individuales.

- [x] 1.1 — Crear composable de mapa de ubicación (`composables/useHotelLocationMap.ts`, `settings/index.vue` migrado, typecheck limpio)
- [x] 1.2 — Fix de centrado del mapa por país (verificado en vivo: cambiar país recentra el iframe, ej. México → 23.6345,-102.5528)
- [x] 1.3 — Shell de la pestaña Ubicación en Página pública (`pages/pagina-publica/ubicacion.vue`, registrada en `pagina-publica-tabs.ts` e `index.vue` para poder verificarla — adelanta parte de 1.6)
- [x] 1.4 — Completar campos de Ubicación (mapa + dirección + coordenadas + provincia/municipio/localidad/CP, verificado en vivo)
- [x] 1.5 — Guardado aislado de Ubicación (verificado en vivo: guarda y persiste sin depender de Configuración → Hotel/Condiciones)
- [x] 1.6 — Registrar la pestaña Ubicación en el menú (hecho junto con 1.3-1.5)
- [x] 1.7 — Mover tipo/estrellas/logo/website a Página pública (nueva SectionCard "Identidad pública" en `general.vue`, con subida de logo propia — mismo patrón de guardado aislado que el resto de la pantalla; verificado en vivo, incluido un bug real encontrado y corregido: `campo || undefined` impedía LIMPIAR un valor ya guardado porque `patchHotel` descarta claves `undefined` del patch — se manda el valor tal cual, vacío incluido)
- [x] **1.7b — Unificar amenities (D3, ver doc 08)**: `getPublicHotelInfo` (`bookingengine/usecases/public-hotel-info.ts`) ahora lee `hotel_amenities` vía un `hotelAmenities` repo opcional (nueva dep al final del constructor de `BookingengineController`, cableada en `bookingengine/index.ts`; cae a `hotel.amenities` si no está seteada, para no romper callers/tests viejos). `pagina-publica/general.vue` dejó de mandar `amenities` dentro de `patchHotel()` y ahora usa `GET`/`PUT /api/amenities/hotel` (`HotelService.amenitiesHotel/saveAmenitiesHotel`) — como su catálogo (20 keys, para destacar en la landing) es un subconjunto reducido del catálogo completo de Configuración → Amenities (35 keys) y ambos ahora escriben a la MISMA tabla, el save hace merge preservando las claves "ajenas" (`foreignAmenityKeys`) para no pisar lo marcado desde Configuración → Amenities — `saveAmenitiesHotel` reemplaza el set completo, así que omitirlas las habría desactivado en cada guardado de esta pantalla. Verificado en vivo: antes del fix el endpoint público devolvía `amenities:[]` (columna JSON vacía) pese a tener 10 amenities activas en `hotel_amenities`; después del fix las devuelve todas, y togglear+guardar una amenity del catálogo reducido agrega/quita solo esa clave sin tocar las demás. Tests nuevos en `public-hotel-info.test.ts` (3 casos) + 4705 tests backend + typecheck + analyze + build frontend, todo verde.
- [x] 1.8 — Sacar la pestaña Ubicación de Configuración (tab, campos y composable removidos de `settings/index.vue`; `país` reubicado a la pestaña Hotel, sigue editable ahí per doc 03; link "Cambiar" de Página pública actualizado a `?tab=hotel`; verificado en vivo y con typecheck/build/analyze)
- [x] **1.4b — Pulido final de `/panel/pagina-publica?tab=ubicacion` (2026-09-08)**: mapa y autocompletado 100% Google Maps (decisión explícita del usuario, sin proveedores alternativos) — API key real con Places+Geocoding habilitadas cargada en `configuration` (dev local). Bugfix real encontrado y corregido: `initInteractiveMap()`/`initAddressAutocomplete()` se llamaban antes de que `loading` pasara a `false`, así que `mapEl`/`addressInputEl` todavía no existían en el DOM y ambas quedaban sin efecto (mapa caía siempre al iframe no interactivo, autocompletado nunca se conectaba) — movidas al `finally`, después de `loading.value = false` + `nextTick()`. Verificado en vivo: mapa interactivo real (clic y arrastre del pin), autocompletado con sugerencias reales de Google Places, selección completa dirección+coordenadas+provincia+municipio, cero errores de consola.
- [x] [1.9 — Migrar tests de ubicación](./done/f1-09-migrar-tests-ubicacion.md) — `settings-location-fields.test.ts` y `settings-geocoding.test.ts` (obsoletos por 1.8: probaban la pestaña Ubicación que ya no existe en `settings/index.vue`) se migraron a `pagina-publica/ubicacion-fields.test.ts` y `pagina-publica/ubicacion-geocoding.test.ts`, montando `ubicacion.vue` directo (sin navegar pestañas). El `TypeError: WeakMap keys must be objects...` de la nota anterior **era falsa alarma**: no es un bug del proyecto, era correr `bun test` (runner nativo de Bun) en vez de `bun run test` → `vitest run` (el runner real configurado en `package.json`/`vitest.config.ts`) — bajo vitest los 8 tests de `settings-plan-pin.test.ts` pasaban siempre. 2 casos de geocoding ("no pisa lo que el usuario escribió a mano") se retiraron en vez de migrarse: desde 1.4b los campos Provincia/Municipio/Localidad/CP son `disabled` con `:value` (no `v-model`) en `ubicacion.vue` — ya no hay forma de tipear un valor a mano en esa UI, el escenario quedó estructuralmente imposible (documentado inline en el test). Verificado: 16 tests nuevos + toda la suite frontend (1204 tests, 6 fallos preexistentes y no relacionados en `CashRegisterView.test.ts` — reproducidos igual en el árbol sin mis cambios, formato de miles de `Intl.NumberFormat` en esta máquina) + typecheck + build, todo verde.
- [x] [1.10 — Gate de verificación F1](./done/f1-10-gate-verificacion.md) — backend: `typecheck` limpio, `arckode analyze` → 0 violaciones, `bun test` → 4705 pass / 0 fail (448 archivos). Frontend: `typecheck` limpio, `vite build` → "✓ built", suite de tests → 1204 pass (6 fallos preexistentes en `CashRegisterView.test.ts`, no relacionados — reproducidos igual en el árbol sin los cambios de este refactor, formato de miles de `Intl.NumberFormat` de esta máquina). Verificación manual en vivo (Playwright, con la API key real de Google Maps): `/panel/pagina-publica?tab=ubicacion` — mapa interactivo carga, pegar coordenadas mueve el pin y dispara un geocode REAL contra la Geocoding API (Provincia/Municipio/Localidad/CP se autocompletan), Guardar persiste, recarga completa de página confirma que los 6 campos sobrevivieron intactos. Cero errores de consola.

## F2 — Backend: pasos de perfil dentro de `OnboardingStep[]`

- [x] [2.1 — Agregar el campo `kind` a `OnboardingStep`](./done/f2-01-agregar-campo-kind.md) — `kind: 'profile' | 'external'` agregado; los 4 pasos operativos (`rooms`/`rates`/`channels`/`team`) marcados `'external'` sin tocar su lógica de `done`.
- [x] [2.2 — Paso `bienvenida`](./done/f2-02-paso-bienvenida.md) — reemplaza al viejo paso `hotel` (mal calibrado). `done`: `name && country && phone && email && ownerNameResuelto` (owner = `users.name` del `role:'hotel_admin'`, NO `hotels.ownerName` — doc 02 sección B).
- [x] [2.3 — Paso `identidad`](./done/f2-03-paso-identidad.md) — `done: accommodationType && currency`. Decisión documentada inline: el default de columna ('hotel'/'USD') cuenta como hecho (no es un dato fiscal sesgado como impuestos).
- [x] [2.4 — Paso `contacto`](./done/f2-04-paso-contacto.md) — `done: phone2 || website || ownerTaxId`, `required: false`, nunca bloquea `completed`.
- [x] [2.5 — Paso `ubicacion`](./done/f2-05-paso-ubicacion.md) — `done: address && latitude && longitude`, con test explícito de que `latitude/longitude` en su default `0` da `false` (falsy).
- [x] [2.6 — Paso `politicas`](./done/f2-06-paso-politicas.md) — `done: taxName && taxRate`. Mismo criterio que identidad (default cuenta como hecho) — la mitigación real del sesgo a RD (riesgo R1) queda documentada como responsabilidad de `StepPoliticas.vue` (F3.9), no de este flag.
- [x] [2.7 — Paso `amenities`](./done/f2-07-paso-amenities.md) — `done: hotel_amenities.length > 0 || childPolicyTocada` (fuente D3: tabla real, no `hotels.amenities`). `required: false`.
- [x] [2.8 — Reordenar `steps[]`](./done/f2-08-reordenar-steps.md) — orden final: bienvenida→identidad→contacto→ubicacion→politicas→amenities→rooms→rates→channels→team (10 pasos).
- [x] [2.9 — Tests backend de los pasos nuevos](./done/f2-09-tests-backend-pasos.md) — `onboarding.test.ts` reescrito: 28 tests (antes 12), cubre cada paso nuevo individualmente + combinaciones (perfil 0%, perfil 100% requerido con opcionales pendientes, los 10 pasos hechos).
- [x] [2.10 — Gate de verificación F2](./done/f2-10-gate-verificacion.md) — backend: `typecheck` limpio, `arckode analyze` → 0 violaciones (hubo que recortar comentarios de `subscriptions/service.ts`, que había cruzado las 200 líneas — límite God Object del analyzer — al agregar los 2 repos nuevos), `bun test` → 4721 pass / 0 fail (448 archivos, +16 vs. antes de F2). Frontend: `typecheck` limpio, `vite build` → "✓ built" (sin cambios de frontend en esta fase, como esperado). `GET /api/onboarding/status` probado en vivo (curl, hotel demo `solmios-corp`) — 10 pasos en el orden correcto, cada uno con `kind`, `doneCount`/`totalCount`/`completed` coherentes con los datos reales del hotel.

## F3 — El Centro de configuración

- [x] [3.1 — Composable `useOnboardingStep`](./done/f3-01-composable-use-onboarding-step.md) — `frontend/src/composables/useOnboardingStep.ts`: `{saving, error, save()}`, protección de doble submit, refresca `GET /api/onboarding/status` al terminar. 5 tests unitarios.
- [x] [3.2 — Ruta `/panel/configuracion-inicial`](./done/f3-02-ruta-configuracion-inicial.md) — agregada en `router/index.ts` con `meta:{requiresHotelAdmin:true}`, mismo patrón que `pagina-publica`.
- [x] [3.3 — Shell del Centro de configuración](./done/f3-03-shell-centro-configuracion.md) — `pages/configuracion-inicial/index.vue`: cabecera con progreso combinado, `onMounted` pide el status, 10 pasos colapsados con check/badge visibles sin expandir. **Corrección posterior (revisión post-F5)**: la cabecera inicialmente usaba `doneCount`/`totalCount` del backend (todos los pasos, incluidos opcionales) — daba un % DISTINTO al de `ProfileProgressBar.vue` para el mismo hotel (ej. 80% en el dashboard, 60% acá), que el usuario reportó como inconsistente. Unificado: acá también cuenta SOLO pasos requeridos, mismo criterio que la franja del dashboard (ver nota en 4.1).
- [x] [3.4 — Expandir/colapsar pasos](./done/f3-04-expandir-colapsar-pasos.md) — `open` como valor único (no Set), transición de alto+fade ~200ms, respeta `prefers-reduced-motion`. A diferencia de `OnboardingGuide.vue`, un paso YA HECHO también se puede reabrir (acá el objetivo es completar/editar, no solo leer). Verificado en vivo y con test.
- [x] [3.5 — `StepBienvenida.vue`](./done/f3-05-step-bienvenida.md) — name/country/phone/email/ownerName pre-cargados. `phone` con validación de campo REQUERIDA del lado del cliente (doc 08 D1) — guardar sin teléfono no dispara ningún request y muestra el error inline; verificado en vivo (DB intacta tras el intento). `ownerName` guarda en `users.name` vía `PUT /api/auth/me` (nuevo `AuthService.updateMe`), no en `hotels.ownerName`.
- [x] [3.6 — `StepIdentidad.vue`](./done/f3-06-step-identidad.md) — **alcance reducido a propósito** (documentado en el código y acá): solo `accommodationType`+`currency` (lo que realmente determina `identidad.done`). Estrellas/logo NO se duplican acá — ya viven en Página pública → General (1.7); zona horaria/check-in-out NO se duplican — ya viven en Configuración → Hotel. Agregarlos habría creado una TERCERA superficie editando los mismos campos, el mismo patrón que causó el bug de amenities duplicadas (D3). Desviación del mockup de doc 05, no de lo que el backend efectivamente valida.
- [x] [3.7 — `StepContacto.vue`](./done/f3-07-step-contacto.md) — phone2/website/ownerTaxId + botón "Saltear" (colapsa sin persistir, sin error) además de "Guardar". Verificado en vivo.
- [x] [3.8 — `StepUbicacion.vue`](./done/f3-08-step-ubicacion.md) — reusa `useHotelLocationMap` (1.1), versión compacta del mismo formulario de Página pública → Ubicación, mismo endpoint/campos — un guardado desde acá es indistinguible de uno hecho allá (misma fuente de datos, no hay sincronización que pueda desalinearse). Verificado en vivo: mapa interactivo carga sin errores de consola.
- [x] [3.9 — `StepPoliticas.vue`](./done/f3-09-step-politicas.md) — envuelve `<CancellationPolicyEditor>` tal cual. Aviso explícito de impuestos (riesgo R1) cuando el país no es RD Y los valores siguen siendo el default ITBIS/18% — verificado en vivo contra el hotel demo (país "Estados Unidos" con default RD sin tocar → aviso visible).
- [x] [3.10 — `StepAmenities.vue`](./done/f3-10-step-amenities.md) — chips de amenities (mismo catálogo/tabla que Configuración → Amenities, D3) + toggle "Acepta niños" con `maxChildAge`/`maxFreeAge` (mismo criterio de validación que `settings/index.vue`: la edad sin cargo no puede superar la edad máxima). Verificado en vivo.
- [x] [3.11 — `StepExternal.vue`](./done/f3-11-step-external.md) — un solo componente genérico para rooms/rates/channels/team, reusa el copy de `onboarding.ts` (how/impact/route/cta), botón navega a la pantalla real.
- [x] [3.12 — Animación de cierre al 100%](./done/f3-12-animacion-cierre.md) — `justCompleted`/`wasCompleted` en `index.vue`: `wasCompleted` se inicializa con el status de la CARGA inicial, así que si el hotel ya estaba 100% al entrar, la animación NO se dispara (solo se activa en la transición false→true dentro de la sesión). Respeta `prefers-reduced-motion`.
- [x] [3.13 — Manejo de error por paso](./done/f3-13-manejo-error-por-paso.md) — verificado en vivo: guardar con `name` vacío → error real del backend ("name: Minimum 2 characters") visible en ESE paso, sin colapsarlo, dato tipeado no se pierde, hotel intacto en DB, reintentar sin recargar funciona.
- [x] [3.14 — Copy final de cada paso](./done/f3-14-copy-final.md) — usuario aprobó la idea base (2026-09-08) con una corrección: pasar todo a **español neutro, registro "usted"** (mismo tono que se aplicó recién en finanzas — "tono usted"). Se corrigió el copy de los 6 pasos de perfil nuevos Y, por decisión explícita del usuario, TAMBIÉN el de los 4 operativos existentes (`rooms`/`rates`/`channels`/`team`, que tenían voseo desde antes de este refactor) — los 10 títulos/descripciones/how/impact de `onboarding.ts`, más los textos propios de cada `Step*.vue` (placeholders, avisos, botones) y el widget `OnboardingGuide.vue` del dashboard (mismo dato, muestra el copy en vivo hoy). Verificado en vivo (Playwright): los 10 títulos y la cabecera del acordeón en "usted", el widget del dashboard también. **Nota residual, fuera de este alcance**: `pagina-publica/ubicacion.vue` (de F1, tarea 1.4) sigue en voseo ("Buscá la dirección…") — no se tocó por estar fuera de los "6 steps + 4 operativos" que el usuario acotó; si se quiere consistencia total del panel, es un cambio aparte.
- [x] [3.15 — Tests del Centro de configuración](./done/f3-15-tests-centro-configuracion.md) — `configuracion-inicial/index.test.ts` (7 tests: orden de los 10 pasos, progreso, badges, invariante de un solo paso abierto — incluye el auto-open del primer pendiente al montar) + `useOnboardingStep.test.ts` (5 tests). Los 10 steps se mockean por completo (`vi.mock` de cada `.vue`) para que el archivo pruebe SOLO la lógica del acordeón — el contenido de cada step ya se verificó en vivo.
- [x] [3.16 — Gate de verificación F3](./done/f3-16-gate-verificacion.md) — backend sin cambios en esta fase (ya verde desde F2). Frontend: `typecheck` limpio, `vite build` → "✓ built", suite de tests → 1224 pass (mismos 4 fallos preexistentes y no relacionados de `CashRegisterView.test.ts`). Verificación manual en vivo (Playwright): shell completo con datos reales del hotel demo, guardado+colapso+progreso en Contacto, mapa interactivo en Ubicación sin errores, aviso R1 en Políticas con un hotel fuera de RD, validación de teléfono en Bienvenida, botón Saltear en Contacto, campos de edad en Amenities — cero errores de consola en todo el recorrido.

## F4 — Reemplazar el widget del dashboard

- [x] [4.1 — `ProfileProgressBar.vue`](./done/f4-01-profile-progress-bar.md) — `frontend/src/components/features/dashboard/ProfileProgressBar.vue`: franja de una línea, texto "Configuración: {pct}% completa" ("Empiece por acá" en 0%), barra fina cyan/white-15, botón "Completar →". El % se calcula SOLO sobre pasos `required` (doc 04: los opcionales no cuentan ni en el numerador ni en el denominador — distinto de `doneCount`/`totalCount` del backend, que sí incluye todo). 4 tests: 0%/parcial/100%/opcionales-no-cuentan.
- [x] [4.2 — Reemplazar `OnboardingGuide` en el dashboard](./done/f4-02-reemplazar-en-dashboard.md) — `dashboard/index.vue:4` cambiado de `<OnboardingGuide />` a `<ProfileProgressBar />`, misma posición (arriba de `CommandCenterHeader`).
- [x] [4.3 — Retirar `OnboardingGuide.vue`](./done/f4-03-retirar-onboarding-guide.md) — `rg OnboardingGuide` confirmó cero imports reales (solo comentarios/historia en `configuracion-inicial/`) → archivo borrado (`git rm`).
- [x] [4.4 — Verificación visual de los 3 estados](./done/f4-04-verificacion-visual.md) — verificado en vivo (Playwright) contra el hotel demo: **parcial** (80% real, 4/5 requeridos — falta `rooms`), **100%** (se creó una habitación de prueba vía API → franja desaparece sin resabio, incluso recargando la página → sigue sin aparecer → habitación de prueba borrada, restaurado a 80%). **0%** verificado con test unitario dedicado (`ProfileProgressBar.test.ts`) en vez de forzarlo en la DB demo — vaciar los 5 campos requeridos de un hotel real y garantizar su restauración exacta es más riesgoso que el valor que agrega frente a un test que ya fija ese comportamiento exacto. Botón "Completar →" verificado apuntando a `/panel/configuracion-inicial`. Cero errores de consola en todo el recorrido.
- [x] [4.5 — Gate de verificación F4](./done/f4-05-gate-verificacion.md) — backend: `typecheck` limpio, `arckode analyze` → 0 violaciones (sin cambios de backend en esta fase). Frontend: `typecheck` limpio, `vite build` → "✓ built", suite de tests → 1228 pass (mismos 4 fallos preexistentes y no relacionados de `CashRegisterView.test.ts`).

## F5 — Limpieza y documentación

- [x] [5.1 — Actualizar CLAUDE.md](./done/f5-01-actualizar-claude-md.md) — nueva entrada en "Estado SDD" (raíz del repo), mismo formato que las entradas existentes: F0-F5 completas, deuda residual (voseo en `pagina-publica/ubicacion.vue`, fuera del alcance de la conversión a "usted").
- [x] [5.2 — Documentar la resolución de amenities duplicadas](./done/f5-02-documentar-resolucion-amenities.md) — ya documentado en profundidad desde 1.7b (código: `public-hotel-info.ts`, `onboarding.ts`; docs: `08-decisiones-abiertas-y-riesgos.md` D3). Se cerró el único checklist suelto que quedaba sin marcar (`07-plan-fases-implementacion.md` Fase 5) — D3 no queda como pregunta abierta en ningún lado del plan.
- [x] [5.3 — Gate final completo](./done/f5-03-gate-final.md) — backend: `typecheck` limpio, `arckode analyze` → 0 violaciones, `bun test` → 4721 pass / 0 fail (448 archivos). Frontend: `typecheck` limpio, `vite build` → "✓ built". Gate final antes de mergear, en verde.

## Revisión post-F5 (2026-09-08) — bugs encontrados y corregidos

A pedido del usuario, revisión completa de todos los archivos tocados en F1-F5 buscando bugs. Encontrados y corregidos 6, todos verificados en vivo:

1. **`StepBienvenida.vue` / `StepPoliticas.vue` no normalizaban `country`** — cargaban `h.country` crudo en vez de `countryName(h.country) || h.country || ''` (patrón usado en `settings/index.vue`/`ubicacion.vue`). En `StepPoliticas` esto tenía consecuencia funcional real: el aviso R1 (impuesto sesgado a RD) comparaba el país SIN normalizar — un hotel dominicano guardado con el ISO viejo ('DO') disparaba el aviso en FALSO. Verificado en vivo con `country='DO'` antes/después.
2. **`StepAmenities.vue` con catálogo de labels incompleto** — el catálogo real tiene 37 keys, el mapa de labels solo cubría 14; el resto se mostraba como key cruda (`kids_playground`, `coffee_maker`, etc.). Completado copiando el mapa entero desde `settings/index.vue` (fuente canónica).
3. **Race condition en el acordeón** (`configuracion-inicial/index.vue`) — si se guardaba el paso A y se cambiaba al paso B antes de que el request de A resolviera, el auto-colapso post-guardado le cerraba el panel a B (que el usuario no tocó) en vez de A. Fix: pasar la key del paso explícita desde el scope del `v-for` en vez de leer `open.value` después de que la promesa resuelve.
4. **`settings/index.vue`: `saveAll()` podía bloquearse por campos ya movidos a otras pantallas** — `HOTEL_RULES` (compartido con `ubicacion.vue`) todavía declara `website`/`logo` (movidos en 1.7) y `address`/`province`/`municipality`/`locality`/`postalCode`/`latitude`/`longitude` (movidos en 1.8); `form.value` los sigue cargando del backend aunque ya no tengan input visible acá. `saveAll()` validaba el set COMPLETO de `HOTEL_RULES` contra esos valores viejos — un hotel con, ej., un `website` en formato inválido tenía el guardado ENTERO de Hotel/Condiciones bloqueado, sin forma de ver ni corregir el campo culpable (ya no está en esta pantalla). Reproducido y confirmado el fix en vivo con una petición PUT real (200 OK con el fix, bloqueado sin él). Fix: filtrar `HOTEL_RULES` a las keys presentes en `FIELD_TAB` (que ya listaba correctamente los campos activos) antes de validar.
5. **Discrepancia de % entre el dashboard y el Centro de configuración** (reportada por el usuario en vivo: 80% en la franja, 60% al entrar) — la cabecera de `configuracion-inicial/index.vue` usaba `doneCount`/`totalCount` del backend (TODOS los pasos, incluidos opcionales), mientras `ProfileProgressBar.vue` ya contaba solo requeridos (doc 04). Dos números "correctos" para lo que medían cada uno, pero la experiencia de ver el % bajar al hacer clic en "Completar" leía como un bug. Decisión del usuario: unificar ambos al criterio de solo-requeridos. Ambas pantallas muestran 80% ahora para el mismo hotel.

Gate completo re-verificado en verde después de cada fix (backend typecheck/analyze, frontend typecheck/build/tests — 1228+ tests, mismos 4 fallos preexistentes no relacionados de `CashRegisterView.test.ts`).

**F0 a F5 completas — el refactor de configuración inicial del hotel queda cerrado.**
