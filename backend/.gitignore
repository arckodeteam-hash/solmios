# Secrets — NUNCA commitear
.env
.env.*
!.env.example

# Base de datos con datos reales
data/
*.db
*.db-shm
*.db-wal

# Logs
*.log

# Node/Bun
node_modules/
# …y también si alguien deja un SYMLINK con ese nombre: `node_modules/` sólo matchea
# directorios, y un symlink se coló en el commit 114efca9 y rompió el deploy (#239).
node_modules
