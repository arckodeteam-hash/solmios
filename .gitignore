# Dependencies
node_modules/
bun.lockb

# Database
*.db
*.db-journal
*.db-wal
*.db-shm
/backend/data/

# Environment
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Build
dist/
build/
.cache/

# Logs
*.log
npm-debug.log*

# Temp
tmp/
temp/
.playwright-cli

# Uploads (evidencia fotográfica de housekeeping y otros archivos servidos)
uploads/

# Playwright/screenshots scratch sueltos en raíz (anclado a raíz: no recursivo, no toca subdirs legítimos)
/backend/*.png
/backend/*.yml
/frontend/*.png
/frontend/*.yml

# Basura local (cache playwright, screenshots sueltos en raíz)
/.playwright-mcp/
/*.png

# GitLab sync state (cada dev tiene su propio estado)
openspec/.gitlab-sync.json

# App móvil Flutter — vive en su propio repo: underworf1/solmios-mobile
/mobile/

# Claude Code — loop runtime state y worktrees (por dev, no commitear)
.claude/state/
.claude/worktrees/
.loopkit/state/
.loopkit/**/__pycache__/
# Copia del instalador (sin punto): mismo criterio que la instalada.
loopkit/state/
loopkit/**/__pycache__/
.loopkit/core
.loopkit/adapters
.loopkit/test
.loopkit/kit-path

# --- LoopKit (andamiaje local, no va al repo) ---
.loopkit/
loopkit/
.claude/hooks/lk-*
.claude/agents/lk-*
.claude/commands/tarea.md
.claude/commands/bug.md
.claude/commands/verificar.md
.claude/settings.local.json
.opencode/
CLAUDE.local.md
AGENTS.local.md

# Reportes de sesión generados en la raíz (no son artefactos del producto)
reporte-*.md
